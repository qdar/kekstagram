import {picturesSection, renderThumbnails} from './thumbnail.js';
const bigPicture = document.querySelector('.big-picture');

const renderBigPicture = (renderThumbnails(PHOTO_COUNT)) => {
  // const onBigPictureOpen = picturesSection.addEventListener('click', (evt) => {
  //   bigPicture.classList.remove('hidden');

    // const thumbnail = evt.target.closest('[data-thumbnail-id]');

    // const picture = pictures.find( (item) => item.id === +thumbnail.dataset.thumbnailId);

    // showBigPicture(picture);
    // );

  // });
};

export {renderBigPicture };

